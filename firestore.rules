rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for development and automated verification environments
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
